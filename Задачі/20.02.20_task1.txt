---------------1----20.2---------------
-- 1. Ќапишите запрос, который выберет все р€ды из таблицы заказов (orders) за март-апрель

-- 2. ¬ыберите всех клиентов (таблица customers) продавцов Peel и Motika.

-- 3. —оставьте запрос, который выведет всех клиентов, чьи имена наход€тс€ в диапазоне A-G включительно.

-- 4. ¬ыберите всех представителей, чьи имена начинаютс€ с латинской литеры "C".

-- 5. ¬ыберите всех представителей, чьи имена начинаютс€ на латинскую литеру "D" и при этом заканчиваютс€ на латинскую литеру "n". ¬ыборка проведите 2-м€ различными способами (2 различных запроса, дающих одинаковый результат).

-- 6. ¬ыберите всех представителей, чьи имена заканчиваютс€ на латинскую литеру "n", но при этом не начинаютс€ на латинскую литеру "D".

-- 7. ¬ыберите все р€ды с NULL-значени€ми из таблицы продавцов.

---------------------------2-----20.02----------------

-- 1. ѕолучите среднее значение пол€ amt (таблица orders), не использу€ при этом функцию AVG(). –езультат должен быть получен одним запросом. ѕодсказка: используйте вложенный запрос.

-- 2. ѕолучите сумму всех продаж продавца с идентификатором 1007. ѕопробуйте составить 2 запроса, получающих одинаковый результат.

-- 3. ѕолучите список городов (без повторов) и максимальный рейтинг дл€ каждого из них из таблицы customers.

-- 4. ѕолучите список городов (без повторов) и минимальный размер комиссионных дл€ каждого из них из таблицы salers.

-----------------------------------------

